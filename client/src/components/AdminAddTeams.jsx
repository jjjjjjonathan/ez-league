import { useState } from 'react';
import CSVReader from './CSVReader';

const AdminAddTeams = (props) => {
  const [teamList, setTeamList] = useState([]);

  return <CSVReader />;
};

export default AdminAddTeams;
